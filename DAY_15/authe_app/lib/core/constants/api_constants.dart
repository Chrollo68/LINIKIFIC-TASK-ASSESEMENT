class ApiConstants {
  /// Base URL for all API endpoints.
  ///
  /// Replace with your actual auth API base URL. The current value
  /// is a placeholder and does not support "/auth/login" or
  /// "/auth/register" routes.
  static const String baseUrl = "https://jwt-demo-api.example.com/api";
}
